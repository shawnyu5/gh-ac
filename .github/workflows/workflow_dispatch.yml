name: Dispatch Hello World

on:
  workflow_dispatch:
    inputs:
      name:
        type: string
        description: THE name

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello world
